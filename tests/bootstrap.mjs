import logger from '../src/libs/logger/index.js'
import { consoleTransport } from '../src/libs/logger/index.js'

beforeAll(() => {
  // logger.transports['consoleTransport'].silent = true
})

afterAll(() => {
  
})