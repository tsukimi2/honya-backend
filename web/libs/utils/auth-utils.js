/*
import cookie from 'cookie-cutter'

export const isAuthenticated = () => {
  let result = false
  try {
    result = cookie.get('loginHash') ? true : false

  } catch(err) {
    console.log('err')
    console.log(err)
  }

  return result
}
*/
